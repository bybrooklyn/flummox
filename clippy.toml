# No relaxations.
#
# Clippy can allow unwrap, expect, panic and indexing inside #[test] code. The
# defaults for those knobs are off and they stay off, so test code is held to
# the same bar as everything else. Tests return `testutil::TestResult` and
# report failures with `check` and `check_eq`.
